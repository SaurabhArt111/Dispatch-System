import {NavLink,Outlet} from "react-router-dom";import {LayoutDashboard,LogOut,Monitor} from "lucide-react";import {useAuth} from "../context/AuthContext";export default function Layout(){const {user,logout}=useAuth();return <div className="app-shell"><aside className="sidebar"><div className="brand">DISPATCH<span>.</span></div><nav><NavLink to="/dashboard"><LayoutDashboard size={18}/>Dashboard</NavLink><NavLink to="/godown-screen/S-28"><Monitor size={18}/>Godown Screen</NavLink></nav><div className="sidebar-user"><strong>{user?.name}</strong><small>{user?.role}</small><button className="ghost-btn" onClick={logout}><LogOut size={16}/>Logout</button></div></aside><main className="main-content"><Outlet/></main></div>}
