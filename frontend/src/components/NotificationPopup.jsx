import {useEffect,useState} from "react";export default function NotificationPopup({event}){const [show,setShow]=useState(false);useEffect(()=>{if(!event)return;setShow(true);const t=setTimeout(()=>setShow(false),7000);return()=>clearTimeout(t)},[event]);if(!show)return null;return <div className="notification-popup"><div className="notification-icon">!</div><div><b>{event.title}</b><p>{event.message}</p></div><button onClick={()=>setShow(false)}>×</button></div>}
